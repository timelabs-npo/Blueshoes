# FreeBSD Packaging

This directory contains the `Makefile` and build scripts required to package `bs-edge-agent` into an `.ipk` for FreeBSD.

## Requirements
- FreeBSD SDK (target: `mediatek/filogic` for MT7981B).
- Rust cross-compilation toolchain.

*Packaging scripts will be added in upcoming Phase 1 pull requests.*
